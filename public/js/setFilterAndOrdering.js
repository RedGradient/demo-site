let currentURL = window.location.href;
let targetButton = Document.getElementById('sort');

function addKey(e) {
    let pressed = Document.getElementById(e.getId);
    if (targetButton.getAttribute('href'));
    // currentURL +=
}
